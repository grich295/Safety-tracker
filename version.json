{
  "version": "2.10.46",
  "build_id": "v21046-clean-baseline-20260918",
  "clean": true,
  "notes": "Housekeeping stage 3: consolidates the six active hotfix scripts into one current baseline file, hotfix-v21046-baseline.js, while keeping app-v21028.js, bulk import and demo files separate so startup behaviour remains close to the proven live architecture. index.html now loads five local app scripts instead of ten, and the offline service-worker shell caches the same reduced active set. No database, workflow, permission or navigation logic is intentionally changed. The old individual hotfix files may remain in the repository for now but are no longer referenced by the live app; they can be archived in the final repository-cleanup stage after this build is verified."
}
