{
  "version": "2.10.31",
  "build_id": "v21031-ptw-stability-20260916",
  "clean": true,
  "notes": "PTW stability fix. Removes the MutationObserver hidden-attribute storm when PTW triggers are checked, opens the PTW form from already-loaded app data instead of waiting on public_config, performs asbestos/location preview from current loaded register data, and opens post-submit PTW review by loading only the selected permit instead of reloading the entire Safety Tracker. No SQL migration required."
}
