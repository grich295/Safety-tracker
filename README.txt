Safety Tracker v2.10.6 - Creator modal fix

Fix:
- After "Analyse & save draft", the questionnaire dialog now closes first.
- The recommendation/result screen then reopens as a fresh top-layer dialog.
- Modal scroll position is reset to the top.
- Save button shows "Saving draft…" while the database save runs.
- New app/CSS filenames force GitHub Pages/mobile cache to load this fix.

Upload all 3 files in this patch to the ROOT of the Safety-tracker GitHub repo:
- index.html
- app-v2106.js
- styles-v2106.css

No SQL required.
